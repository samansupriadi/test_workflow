name: Deploy To Production
on:
  pull_request:
    branches:
      - staging
      - development
    types:
      - closed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check user privilege
        run: |
          sleep 60
